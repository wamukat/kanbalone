name: kanbalone

services:
  kanbalone:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 3000
      KANBALONE_DB_FILE: /app/data/kanbalone.sqlite
    ports:
      - "${KANBAN_PORT:-3000}:3000"
    volumes:
      - kanbalone-data:/app/data
    restart: unless-stopped

volumes:
  kanbalone-data:
